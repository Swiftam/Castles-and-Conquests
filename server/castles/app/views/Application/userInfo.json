{
    "name": "${user.name}",
    "level": "${user.level.rank}",
    "health": "${user.health}",
    "healthMax": "${user.healthMax}",
    "gold": "${user.gold}",
    "xp": "${user.exp}",
    "xpGoal": "${user.getExpToLevel()}"
}