{
    "name": "${user.name}",
    "level": "${user.level}",
    "health": "${user.health}",
    "healthMax": "${user.healthMax}",
    "gold": "${user.gold}",
    "xp": "${user.exp}",
    "xpGoal": "${level.xp}"
}